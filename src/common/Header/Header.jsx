import styles from "./Header.module.scss";
import clsx from "clsx";

export const Header = () => {
  return (
    <header>
      <section className={clsx("container")}></section>
    </header>
  );
};
