import styles from "./HomePage.module.scss";
import clsx from "clsx";

export const HomePage = () => {
  return (
    <main>
      <section className={clsx("container")}>HomePage</section>
    </main>
  );
};
